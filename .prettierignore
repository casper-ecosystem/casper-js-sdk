dist/
docs/
node_modules/
site/

# Generated by api-extractor and compared byte-for-byte in CI, so reformatting it
# would fail the gate it exists for — and lint-staged runs prettier over *.md.
etc/
temp/
