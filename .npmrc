ignore-scripts=true
# Enforced from npm 11.10 only, and never by `npm ci` (that resolves from the
# lockfile). It bites when someone adds or bumps a dependency by hand — and any
# command that re-resolves the whole tree against the registry, which is why CI
# turns it off for the `npm audit signatures` step.
min-release-age=7
