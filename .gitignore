node_modules

# Output
/docs/
/dist/

# api-extractor's scratch copy of the API report. The reviewed one is /etc/.
/temp/

# Internal working documents — never committed, never published
/internal-docs/

.eslintcache
.nyc_output
/coverage/

.DS_Store

.env
.idea

# Extracted from the nctl container by e2e/run.sh — a private key, never committable.
e2e/faucet_secret_key.pem
